# orchestrai-api-server

Quarkus REST API + SSE streaming entry point.

## TODO

- Flow/Execution/Secret/Plugin/Metrics REST resources
- SSE: logs + token streams
- Kafka: publish executions, consume task-logs
- JWT + RBAC
- See `docs/10-api-design.md`
