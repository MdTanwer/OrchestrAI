# orchestrai-yaml-parser

Parse and validate flow YAML into domain models.

## TODO

- `FlowParser` — Jackson YAML → Flow
- `FlowValidator` — schema + plugin type validation
- See `docs/04-yaml-schema.md`
