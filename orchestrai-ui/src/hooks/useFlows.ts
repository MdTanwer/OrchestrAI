/**
 * TODO: TanStack Query hooks for flows.
 * GET /flows, GET /flows/{namespace}/{id}, POST /flows
 */
'use client';

// TODO: export function useFlows() { return useQuery({ queryKey: ['flows'], queryFn: ... }) }
