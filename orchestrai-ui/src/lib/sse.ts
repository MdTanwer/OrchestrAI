/**
 * TODO: SSE helpers using @microsoft/fetch-event-source.
 * Endpoints:
 *   GET /executions/{id}/logs/stream  — task logs
 *   GET /executions/{id}/stream       — token_delta streaming
 * See docs/10-api-design.md
 */

// TODO: export function subscribeLogStream(executionId: string, onEvent: (data: unknown) => void): AbortController
// TODO: export function subscribeTokenStream(executionId: string, onEvent: (data: unknown) => void): AbortController
