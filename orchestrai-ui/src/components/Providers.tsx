/**
 * TODO: Wrap app with QueryClientProvider (@tanstack/react-query).
 * Add in src/app/layout.tsx
 */
'use client';

// TODO: export function Providers({ children }: { children: React.ReactNode }) { ... }
